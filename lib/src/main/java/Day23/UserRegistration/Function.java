package Day23.UserRegistration;
@FunctionalInterface
public interface Function {
	
	//it having only one abstract method
	//it allows static methods
	//it also having default methods.
	public void check(String FirstName);
}
